# Raw datasets -- large, publicly re-downloadable; see DATASETS.md for sources
dataset/
Eczema/
SkinDisease/
SkinDisNet/

# Trained model weights -- large binaries, regenerate via scripts/
models/

# Reference papers (PDFs) and reports -- not part of the code/docs push
*.pdf
*.docx
~$*.docx

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Build output and training checkpoints -- regenerate via firmware/ and scripts/
firmware/build/
experiments/*/checkpoints/
*.pt
_edge_sim_all_tmp_checkpoint.pt

# Presentations and document backups -- reports stay out of the repo, like *.docx / *.pdf
*.pptx
*.bak
*.bak_*
*.winpath.bak

# Images derived from third-party dataset photos (the labels CSVs are kept)
dead_ends/eczema_severity_proxy_attempt/severity_labels/**/*.jpg

# Local tooling
graphify-out/
.claude/
CLAUDE.md

# Auto-generated CatBoost training output
catboost_info/

# Literature-search API response cache and raw record dump -- regenerate with
# scripts/systematic_search_skintone_compression.py (the screened lists and log are kept)
docs/lit_search_*/api_cache.json
docs/lit_search_*/records_all.csv
